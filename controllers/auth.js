module.exports.login = function (req, res) {
  res.status(200).json({
    loginFromController: true
  })
}

module.exports.register = function (req, res) {
  res.status(200).json({
    loginFromController: true
  })
}